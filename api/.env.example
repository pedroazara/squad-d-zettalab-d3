# Copie para api/.env local (nao versionar) se quiser.
# Com Docker Compose da raiz em execucao:
# docker compose up -d postgres

DATABASE_URL=postgresql+psycopg://postgres:postgres@localhost:5432/guarawatch
