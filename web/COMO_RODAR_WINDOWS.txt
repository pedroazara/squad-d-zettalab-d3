INTEGRACAO FRONT X BACKEND - STATUS REAL DO PROJETO

Resumo curto:
- Hoje o backend esta quase vazio.
- Ja da para integrar somente um endpoint de teste de vida.

==================================================
O QUE JA EXISTE NO BACKEND (PRONTO)
==================================================

Pasta backend:
api/

Arquivo principal:
api/main.py

Endpoint pronto:
GET /health

Resposta esperada:
{
  "status": "ok"
}

URL local completa:
http://localhost:8000/health

Documentacao automatica do FastAPI:
http://localhost:8000/docs

==================================================
O QUE NAO EXISTE AINDA NO BACKEND
==================================================

- Nao existem rotas de login reais.
- Nao existem rotas de dashboard (nacional, estados, ocorrencias etc.).
- Nao existem servicos de banco conectados.
- As pastas api/routes, api/models e api/services estao sem implementacao.

==================================================
O QUE O FRONT ESTA USANDO HOJE
==================================================

- O front usa dados mockados.
- Arquivos principais:
  - src/services/mockAuth.ts
  - src/services/mockData.ts
- Ou seja: tela funciona para demonstracao, mas sem API real ainda.

==================================================
INTEGRACAO QUE JA DA PRA FAZER AGORA
==================================================

Integracao minima (sanidade):
1. Front chamar GET http://localhost:8000/health
2. Se vier {"status":"ok"}, backend esta no ar.
3. A partir disso, criar os proximos endpoints reais.

==================================================
COMO RODAR O BACKEND RAPIDO (WINDOWS)
==================================================

No terminal, dentro da pasta api:

python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
uvicorn main:app --reload

Depois testar no navegador:
http://localhost:8000/health

==================================================
CHECKLIST DE INTEGRACAO
==================================================

[ ] Backend subiu em http://localhost:8000
[ ] /health respondeu {"status":"ok"}
[ ] Front continua usando mocks
[ ] Definidos proximos endpoints reais para substituir os mocks
